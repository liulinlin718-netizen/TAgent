# TAgent 创新落地审查手册

> 本文档是 TAgent 项目的**创新审查标准**。每个功能在设计和实现前，都必须通过此审查。
> 目的：控制创新边界，确保我们的设计**既领先又能落地**，不做纸上谈兵的空想。

---

## 一、审查原则

### 三不原则

| 原则 | 含义 | 举例 |
|------|------|------|
| **不做无参照创新** | 每个"创新点"必须找到至少一个行业参照物——即使我们做得不同，也要证明这条路有人走过 | Session Fork → Warp 已经有 Conversation Forking |
| **不做无约束设计** | 每个架构层必须有明确的约束和降级方案——不能假设理想情况永远成立 | 治理引擎 → 如果策略编译器理解错误怎么办？必须有兜底 |
| **不做无验证规划** | 每个 Phase 的产出必须在该 Phase 内可独立验证——不能"做完 Phase 3 才知道 Phase 1 对不对" | Agent Loop → Phase 1 就必须用单 Agent 跑通完整循环 |

### 创新分级

| 级别 | 定义 | 我们的态度 | 占比目标 |
|------|------|-----------|---------| 
| **L1: 行业已验证** | 竞品已实现并在生产环境运行 | 放心使用，重点做差异化交互 | 60% |
| **L2: 行业有先例** | 有产品做过，但未大规模验证 | 谨慎使用，做好降级方案 | 30% |
| **L3: 原创概念** | 我们首创，行业无参照 | 严格控制，必须在 MVP 中简化验证 | ≤10% |

---

## 二、逐项审查（更新版——基于 references 优化后的 plan）

### 审查格式

| 字段 | 说明 |
|------|------|
| **功能** | plan 中的功能描述 |
| **创新级别** | L1/L2/L3 |
| **行业参照** | 有哪些产品/论文做过类似的事 |
| **落地风险** | 实现时可能遇到的真实问题 |
| **降级方案** | 如果完整版做不出来，最低可接受的替代方案是什么 |
| **审查结论** | ✅ 可行 / ⚠️ 需简化 / ❌ 需重新设计 |

---

### 2.1 Agent 二态模型（常驻 + 任务态）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1 |
| **行业参照** | CrewAI 角色系统、Claude Code Coordinator-Worker、Dify Agent 节点 |
| **落地风险** | 低 |
| **降级方案** | 无需降级 |
| **审查结论** | ✅ 可行 |

---

### 2.2 裂变作为通用能力 + Explore 只读模式（新增）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1 |
| **行业参照** | Claude Code subagent（裂变）+ Claude Code Explore Agent（只读，低成本模型）、CrewAI delegation、LangGraph subgraph |
| **落地风险** | 中。递归裂变需控制深度。Explore 模式本身低风险（只读+便宜模型）|
| **降级方案** | MVP 限制裂变深度 2 层。Explore 模式在 Phase 1 就可实现（单 Agent 内调用低成本模型搜索） |
| **审查结论** | ✅ 可行。Explore 模式是成本优化利器，优先级高 |

---

### 2.3 Agent Loop 7步 + Snapshot + Explore（更新）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1 |
| **行业参照** | Claude Code（Gather→Act→Observe）、Codex CLI（Reasoning→Selection→Execution）、OpenCode（Snapshot 撤销）|
| **落地风险** | 低。Snapshot 本质是将当前状态序列化到文件，技术简单 |
| **降级方案** | 无需降级。Snapshot 不需要全量保存——只保存 Agent 上下文摘要 + 工具状态即可 |
| **审查结论** | ✅ 可行。Snapshot 是安全兜底，必须在 Phase 1 实现 |

---

### 2.4 Agent Card + 多维状态 + SOUL.md + Heartbeat + 三级审批（更新）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1（Agent Card、审批）+ L2（多维状态、Heartbeat） |
| **行业参照** | A2A Agent Card、Decagon Versioning（Agent Card）。Hermes-Team 多维状态。OpenClaw Heartbeat。Codex CLI 三级审批 |
| **落地风险** | 中。Agent Card 本身简单。**多维状态模型增加了 UI 复杂度**——4 个维度各自驱动不同 UI 元素，前端需要处理大量状态组合 |
| **降级方案** | MVP 阶段多维状态只实现 `business`（驱动节点动画）+ `runtime`（健康灯）两个维度。`human_interaction` 和 `orchestration` 延后到 Phase 2 |
| **审查结论** | ⚠️ 需分期。4 维状态同时做太重，Phase 1 只做 2 维 |

> [!WARNING]
> **多维状态的 UI 组合爆炸风险**：4 维度 × 各 3-4 种值 = 可能的组合数极多。前端不可能为每种组合都设计独特表现。建议用**主状态 + 次状态徽章**模式——节点动画由 `business` 驱动，其他维度用小徽章叠加。

---

### 2.5 Observability Trace + JSONL append-only（更新）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1 |
| **行业参照** | LangSmith Trace、Langfuse span、OpenTelemetry。OpenCode JSONL append-only |
| **落地风险** | 低。JSONL append-only 是最简单的日志方案 |
| **降级方案** | 无需降级 |
| **审查结论** | ✅ 可行 |

---

### 2.6 实时通信：SSE + WebSocket 双通道（新增）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1 |
| **行业参照** | Hermes-Team 已验证此方案（SSE 事件 + WebSocket 流式输出） |
| **落地风险** | 低。两种协议都是成熟技术 |
| **降级方案** | 无需降级 |
| **审查结论** | ✅ 可行。经过 Hermes-Team 生产验证 |

---

### 2.7 Agent 间通信 + HumanInputRequest（更新）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1 |
| **行业参照** | A2A Protocol。Hermes-Team `request_human_input()`。Claude Code 隔离上下文+摘要返回 |
| **落地风险** | 低。**隔离上下文+摘要返回**是关键——子 Agent 不共享父对话，避免上下文膨胀 |
| **降级方案** | 无需降级 |
| **审查结论** | ✅ 可行 |

---

### 2.8 浏览器工具集成（新增）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1 |
| **行业参照** | Agent-Browser (Vercel Labs)、OpenClaw Body 层 Playwright |
| **落地风险** | 中。浏览器状态管理（登录态保持、页面加载等待、动态内容）在实际使用中问题多 |
| **降级方案** | Phase 2 先只做**无状态页面抓取**（打开→提取→关闭），不做需要登录的复杂交互。需要登录的场景延后 |
| **审查结论** | ⚠️ 需分期。先做简单抓取，复杂交互延后 |

---

### 2.9 分层 Monorepo + Runtime-UI 解耦（新增）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1 |
| **行业参照** | Pi Framework 四层 Monorepo、Codex CLI App Server 解耦 |
| **落地风险** | 低-中。Monorepo 管理需要配置好构建工具（turborepo/nx），初期有一次性成本 |
| **降级方案** | 如果 Monorepo 工具链太重，Phase 1 可先用简单的目录分离（`src/ai/`, `src/core/`, `src/server/`, `src/web/`），Phase 2 再迁移到真正的 Monorepo |
| **审查结论** | ✅ 可行。但需在 Phase 1 第一周确定 Monorepo 工具（推荐 Turborepo） |

---

### 2.10 Docker 沙箱执行（新增）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1 |
| **行业参照** | Codex CLI（Bubblewrap/Docker）、E2B 沙箱 |
| **落地风险** | 中。Docker 增加部署复杂度。本地开发需要 Docker 环境。冷启动延迟 |
| **降级方案** | MVP 不做 Docker 沙箱。Agent 操作限制在**工作区目录白名单内**（文件系统级），通过 Node.js `child_process` 的 cwd 限制 + 工具权限白名单实现安全隔离。Phase 3+ 再加 Docker |
| **审查结论** | ⚠️ 需延后。Phase 2 用目录白名单替代沙箱，Phase 3+ 加 Docker |

> [!IMPORTANT]
> **不要在 Phase 1-2 引入 Docker 依赖**。它会大幅增加开发者本地环境配置复杂度，与"快速验证"的目标冲突。用简单的文件系统权限限制足以应对 MVP 阶段。

---

### 2.11 三级审批模式（Suggest / Auto-Edit / Full-Auto）（新增）

| 字段 | 内容 |
|------|------|
| **创新级别** | L1 |
| **行业参照** | Codex CLI 三级审批模式，已在生产中验证 |
| **落地风险** | 低。本质是治理引擎的约束严重度配置——Suggest = 全部需确认，Full-Auto = 全部自动 |
| **降级方案** | 无需降级 |
| **审查结论** | ✅ 可行。直接映射到 Agent Card 的 `approval_mode` 字段 |

---

### 2.12 Skills 自学习系统

| 字段 | 内容 |
|------|------|
| **创新级别** | L2 |
| **行业参照** | Hermes Skills、Superpowers Skill Markdown |
| **落地风险** | 中。自动提炼依赖 LLM 准确总结 |
| **降级方案** | MVP 用"Agent 建议 + 用户确认"模式 |
| **审查结论** | ⚠️ 需简化 |

---

### 2.13 Session 三种创建方式

| 字段 | 内容 |
|------|------|
| **创新级别** | L1-L2 混合 |
| **行业参照** | Warp Forking、Pi 树形会话历史、Claude Code worktree |
| **落地风险** | 新建/完整Fork=低。摘要Fork=中-高 |
| **降级方案** | 先做新建 + 完整Fork，摘要Fork延后 |
| **审查结论** | ⚠️ 需分期 |

---

### 2.14 Session Merge 与 Diff

| 字段 | 内容 |
|------|------|
| **创新级别** | L2-L3 混合 |
| **行业参照** | Warp 有 Fork 无 Merge。行业无对话级 Merge 先例 |
| **落地风险** | **高** |
| **降级方案** | 降级为"摘取引用"——用户手动从分支复制结论到主线 |
| **审查结论** | ⚠️ 需重新设计。MVP 做"摘取引用" |

> [!CAUTION]
> Session Merge 是 plan 中**创新度最高但落地风险也最高的功能**。行业中没有产品成功实现过对话级别的自动 Merge。

---

### 2.15 治理引擎——策略编译器

| 字段 | 内容 |
|------|------|
| **创新级别** | L2-L3 |
| **行业参照** | 行业用 Policy-as-Code，非自然语言编译 |
| **落地风险** | **高**。LLM 理解偏差 → 错误治理规则 |
| **降级方案** | MVP 用**预设规则模板 + 参数调整** |
| **审查结论** | ⚠️ 需简化 |

> [!NOTE]
> ✅ **Plan 已采纳此建议**。Phase 2 路线图已改为"预设模板"而非"策略编译器"。通过审查。

---

### 2.16 治理引擎——五类协议体系

| 字段 | 内容 |
|------|------|
| **创新级别** | L2 |
| **行业参照** | 分层预算、硬/软限制、最小权限、AI Gateway |
| **落地风险** | 中。资源/安全已有成熟方案，方向/组织较抽象 |
| **降级方案** | MVP 只做资源 + 安全 |
| **审查结论** | ⚠️ 需分期 |

> [!NOTE]
> ✅ **Plan 已采纳此建议**。Phase 2 路线图写的是"资源/安全协议"，Phase 4 做其余。通过审查。

---

### 2.17 推理轨迹时间线

| 字段 | 内容 |
|------|------|
| **创新级别** | L1-L2 |
| **行业参照** | LangSmith Trace 视图、Langfuse span |
| **落地风险** | 中。面向非技术用户的翻译是创新点 |
| **降级方案** | 硬编码翻译模板 |
| **审查结论** | ✅ 可行 |

---

### 2.18 时间旅行

| 字段 | 内容 |
|------|------|
| **创新级别** | L2 |
| **行业参照** | LangGraph Studio checkpoint、Warp Fork from any point |
| **落地风险** | 中-高 |
| **降级方案** | MVP 只做"回溯查看"，重执行通过手动 Fork |
| **审查结论** | ⚠️ 需简化 |

---

### 2.19 拖拽式管理

| 字段 | 内容 |
|------|------|
| **创新级别** | L2 |
| **行业参照** | Dify、Coze |
| **落地风险** | 低 |
| **审查结论** | ✅ 可行 |

---

## 三、创新落地矩阵（更新版）

```
                    创新度 ←—————————————————————→ 成熟度
                    
  L3 原创          Session Merge (自动合并)
                   
  L2-L3            策略编译器 (自然语言→规则) [已降级为预设模板 ✅]
                   
  L2 有先例        时间旅行 (重执行)    方向/组织治理    Skills 自动提炼
                   摘要 Fork            Docker 沙箱
                   浏览器复杂交互
                   
  L1-L2            多维状态 (4维)       Heartbeat 巡检    推理时间线
                   
  L1 已验证        Agent Loop+Snapshot  Agent Card    Trace JSONL
                   裂变+Explore         SSE+WebSocket 双通道
                   二态模型             Monorepo 分层   三级审批
                   通信协议+HumanInput  资源/安全治理    拖拽管理
                   完整 Fork            浏览器简单抓取
```

### 占比评估

| 级别 | 功能数 | 占比 | 目标 | 状态 |
|------|--------|------|------|------|
| **L1** | 14 | 64% | 60% | ✅ 达标 |
| **L2** | 7 | 32% | 30% | ✅ 达标 |
| **L3** | 1 (Session Merge) | 4% | ≤10% | ✅ 达标 |

---

## 四、本次审查发现的问题

### ❌ 问题 1：Phase 2 路线图仍包含 Docker 沙箱

**位置**：`implementation_plan.md` 5.2 后端技术栈

**问题**：技术栈表中列出了"Docker 容器（← Codex CLI 沙箱模式）"，但 Phase 2 不应引入 Docker 依赖。

**建议行动**：技术栈中保留 Docker 作为**Phase 3+ 目标**，Phase 1-2 用**目录白名单 + 工具权限控制**替代。

### ❌ 问题 2：Phase 3 仍写着"Session Merge 与 Diff 对比"

**位置**：`implementation_plan.md` Phase 3 路线图第 729 行

**问题**：Session Merge 是 L3 功能（创新度最高、落地风险最高），不应在 Phase 3 出现完整版。

**建议行动**：将 Phase 3 中的"Session Merge 与 Diff 对比"改为"Session 结论摘取引用 + Diff 对比视图"。

### ❌ 问题 3：Phase 1 Agent Card 多维状态过重

**位置**：`implementation_plan.md` Phase 1 路线图第 704 行

**问题**："Agent Card 多维状态模型"写在 Phase 1，但 4 维状态同时实现 UI 复杂度太高。

**建议行动**：Phase 1 只实现 `business` + `runtime` 两个维度，Phase 2 加入 `human_interaction` + `orchestration`。

---

## 五、审查条目 Checklist（开发时使用）

每个功能/PR 上线前必须回答以下问题：

### A. 行业对齐检查

- [ ] **有参照吗？** 能否说出至少一个做过类似事情的产品/框架？
- [ ] **我们的差异化是什么？** 不是"我们也做了"，而是"我们做得不一样在哪里"？
- [ ] **行业为什么没这样做？** 如果一个显而易见的功能没有产品实现，要问自己"是他们没想到，还是做过了发现不行？"

### B. 落地可行性检查

- [ ] **能在一个 Sprint 内做出可 demo 的版本吗？** 如果不能，说明粒度太大，需要拆分
- [ ] **有降级方案吗？** 如果完整版做不出来，最低可接受的替代方案是什么？
- [ ] **依赖 LLM 理解力的部分有兜底吗？** LLM 会犯错——所有依赖 LLM 输出做决策的环节，必须有"LLM 理解错了怎么办"的处理
- [ ] **成本可控吗？** 估算过这个功能在典型使用场景下的 token 消耗吗？

### C. 用户价值检查

- [ ] **非技术用户能理解吗？** 用自然语言向一个市场经理解释这个功能，30 秒内能说清楚吗？
- [ ] **用户需要这个吗？** 还是我们觉得很酷所以想做？有没有用户场景需要它？
- [ ] **信息过载了吗？** 这个功能是否会让界面变得更复杂？是否遵循了"渐进式披露"？

### D. 架构一致性检查

- [ ] **符合 Agent Loop 7步+Snapshot 结构吗？** 新功能是否正确 hook 进了某一步？
- [ ] **使用 Trace 数据模型吗？** 可视化功能是否消费 JSONL Trace 数据？
- [ ] **通过 Agent Card 管理吗？** Agent 配置/状态是否在 Agent Card 中定义？
- [ ] **治理引擎能感知吗？** 新增行为是否经过治理检查点？
- [ ] **通过双通道通信吗？** 事件用 SSE，流式用 WebSocket？

### E. 创新边界检查

- [ ] **是 L1/L2/L3 哪个级别？** 如果是 L3，是否有充分理由必须在当前 Phase 实现？
- [ ] **能否用已有方案组合实现？** 在发明新概念之前，检查是否可以通过组合现有成熟方案达到 80% 的效果
- [ ] **这个功能删掉会怎样？** 如果产品仍然成立，说明它不是核心——推迟到后面的 Phase

---

## 六、行动建议汇总

| # | 功能 | 当前 Plan 状态 | 审查结论 | 建议行动 |
|---|------|---------------|---------|---------|
| 1 | Docker 沙箱 | Phase 2 技术栈 | ⚠️ | **延后到 Phase 3+**，MVP 用目录白名单 |
| 2 | Session Merge | Phase 3 "Merge 与 Diff" | ⚠️ | **降级为"摘取引用 + Diff 视图"** |
| 3 | 多维状态 (4维) | Phase 1 全部实现 | ⚠️ | **Phase 1 做 2 维**（business+runtime），Phase 2 加 2 维 |
| 4 | 浏览器工具 | Phase 2 集成 | ⚠️ | Phase 2 只做**无状态抓取**，复杂交互延后 |
| 5 | 策略编译器 | 已改为预设模板 | ✅ | 已采纳，通过 |
| 6 | 五类治理 | Phase 2 资源+安全 | ✅ | 已采纳，通过 |
| 7 | 三级审批 | Phase 2 实现 | ✅ | 直接可做，通过 |
| 8 | Explore 模式 | Phase 1 Agent Loop | ✅ | 低成本高收益，优先实现 |
| 9 | Snapshot 快照 | Phase 1 Agent Loop | ✅ | 安全兜底必备，通过 |
| 10 | SSE+WebSocket | Phase 1 双通道 | ✅ | 经过验证的方案，通过 |
